import React from 'react';

export default class Root extends React.Component {
  render() {
    return (
      <div style={{marginTop: '100px'}}>
        <h2>App 3 - React again</h2>
        This was rendered by app 3, which is written in React.
      </div>
    );
  }
}
